#include "main.h"
/**
  *mul - multiplies two integers
  *@a: first number to be multiplied
  *@b: second number to be multiplied
  *Return: The result of multiplied numbers
  */
int mul(int a, int b)
{

	return (a * b);
}
